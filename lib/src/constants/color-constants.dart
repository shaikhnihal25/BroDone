import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primaryColor = Color(0xFF7670F0); // Teal
  static const secondaryColor = Color(0xFF191970); // Deep Blue

  // Background Colors
  static const backgroundColor = Colors.white;
  static const scaffoldBackgroundColor = Colors.white;

  // Text Colors
  static const textColor = Colors.black;
  static const secondaryTextColor = Colors.grey;

  // Button Colors
  static const buttonColor = primaryColor;
  static const buttonTextColor = Colors.white;

  // Icon Colors
  static const iconColor = primaryColor;

  // Error and Alert Colors
  static const errorColor = Colors.red;
  static const successColor = Colors.green;
  static const alertColor = Colors.amber;
}
